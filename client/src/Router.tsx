import { BrowserRouter } from "react-router-dom";

import App from "./App.tsx";

const Router = () => {
  return (
    <BrowserRouter>
      <App />
    </BrowserRouter>
  );
};

export default Router;
